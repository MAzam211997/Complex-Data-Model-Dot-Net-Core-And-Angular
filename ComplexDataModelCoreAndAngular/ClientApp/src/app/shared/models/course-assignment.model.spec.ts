import { CourseAssignment } from './course-assignment.model';

describe('CourseAssignment', () => {
  it('should create an instance', () => {
    expect(new CourseAssignment()).toBeTruthy();
  });
});
